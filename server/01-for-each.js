const letters = ['a', 'b', 'c'];

/*
// Loop trought array classic method
for (let index = 0; index < letters.length; index++) {
  const element = letters[index];
  console.log(element);
}
*/

letters.forEach(item => console.log(item));